<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Platforms>AnyCPU</Platforms>
    <AssemblyVersion>2.0.26.0</AssemblyVersion>
    <FileVersion>2.0.26.0</FileVersion>
    <Version>2.0.26</Version>
    <RepositoryUrl>https://github.com/islero/High-Performance-Backtest.Net</RepositoryUrl>
    <Description>Reverting concurrent queue</Description>
    <PackageReleaseNotes>Implemented Backtesting Executor, release candidate</PackageReleaseNotes>
    <Configurations>Release;Debug</Configurations>
    <Authors>Yurii Dumanskyi</Authors>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
  </PropertyGroup>

</Project>
